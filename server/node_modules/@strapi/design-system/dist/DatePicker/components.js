import o from "styled-components";
import { Box as t } from "../Box/Box.js";
import { InputWrapper as i } from "../Field/FieldInput.js";
import { Popover as a } from "../Popover/Popover.js";
const l = o(a)`
  max-height: ${3 * 6}rem;
  overflow: hidden;
`, c = o.button`
  border: none;
  background: transparent;
  border-radius: ${({ theme: r }) => r.borderRadius};
  cursor: ${(r) => r["aria-disabled"] ? "not-allowed" : void 0};

  & svg path {
    fill: ${({ theme: r }) => r.colors.neutral500};
  }
`, m = o.div`
  ${({ bold: r, theme: e }) => r ? `& ${i} {
  border: 1px solid ${e.colors.primary600};
}` : void 0}
`, u = o(t)`
  background: transparent;
  border: none;
  position: relative;
  z-index: 1;

  svg {
    height: ${11 / 16}rem;
    width: ${11 / 16}rem;
  }

  svg path {
    fill: ${({ theme: r }) => r.colors.neutral600};
  }
`;
export {
  c as DatePickerButton,
  l as DatePickerPopover,
  m as DatePickerWrapper,
  u as IconBox
};
