const E = (n, s) => {
  const t = new Date().getFullYear(), a = n?.getFullYear() ?? parseInt(t, 10) - 200, A = s?.getFullYear() ?? parseInt(t, 10) + 15, r = [];
  for (let e = a; e <= A; e++)
    r.push(e);
  return r;
};
export {
  E as getYears
};
