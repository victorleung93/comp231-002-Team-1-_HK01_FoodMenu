import { parseDate as t } from "../parseDate.js";
describe("parseDate", () => {
  it("should return a date", () => {
    expect(t("2021-11-05T15:37:29.592Z") instanceof Date).toBeTruthy();
  }), it("should return null if the passed params has not the right format", () => {
    expect(t("test")).toBeNull();
  });
});
