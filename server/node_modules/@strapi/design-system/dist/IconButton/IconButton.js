import { jsxs as $, jsx as e } from "react/jsx-runtime";
import * as a from "react";
import n from "styled-components";
import { BaseButton as b } from "../BaseButton/BaseButton.js";
import { Flex as m } from "../Flex/Flex.js";
import { Tooltip as g } from "../Tooltip/Tooltip.js";
import { VisuallyHidden as v } from "../VisuallyHidden/VisuallyHidden.js";
const I = a.forwardRef(({
  label: r,
  noBorder: i = !1,
  children: d,
  icon: c,
  disabled: o = !1,
  onClick: l,
  "aria-label": u,
  ...f
}, p) => {
  const t = $(s, {
    "aria-disabled": o,
    background: o ? "neutral150" : void 0,
    borderWidth: i ? 0 : void 0,
    justifyContent: "center",
    height: `${2}rem`,
    width: `${2}rem`,
    ...f,
    ref: p,
    onClick: (h) => {
      !o && l && l(h);
    },
    children: [e(v, {
      as: "span",
      children: r ?? u
    }), a.cloneElement(c || d, {
      "aria-hidden": !0,
      focusable: !1
    })]
  });
  return r ? e(g, {
    label: r,
    children: t
  }) : t;
}), s = n(b)`
  svg {
    > g,
    path {
      fill: ${({
  theme: r
}) => r.colors.neutral500};
    }
  }

  &:hover {
    svg {
      > g,
      path {
        fill: ${({
  theme: r
}) => r.colors.neutral600};
      }
    }
  }

  &:active {
    svg {
      > g,
      path {
        fill: ${({
  theme: r
}) => r.colors.neutral400};
      }
    }
  }

  &[aria-disabled='true'] {
    svg {
      path {
        fill: ${({
  theme: r
}) => r.colors.neutral600};
      }
    }
  }
`, w = n(m)`
  & span:first-child button {
    border-left: 1px solid ${({
  theme: r
}) => r.colors.neutral200};
    border-radius: ${({
  theme: r
}) => `${r.borderRadius} 0 0 ${r.borderRadius}`};
  }

  & span:last-child button {
    border-radius: ${({
  theme: r
}) => `0 ${r.borderRadius} ${r.borderRadius} 0`};
  }

  & ${s} {
    border-radius: 0;
    border-left: none;

    svg {
      path {
        fill: ${({
  theme: r
}) => r.colors.neutral700};
      }
    }

    &:hover {
      background-color: ${({
  theme: r
}) => r.colors.neutral100};

      svg {
        path {
          fill: ${({
  theme: r
}) => r.colors.neutral800};
        }
      }
    }

    &:active {
      background-color: ${({
  theme: r
}) => r.colors.neutral150};
      svg {
        path {
          fill: ${({
  theme: r
}) => r.colors.neutral900};
        }
      }
    }

    &[aria-disabled='true'] {
      svg {
        path {
          fill: ${({
  theme: r
}) => r.colors.neutral600};
        }
      }
    }
  }
`;
export {
  I as IconButton,
  w as IconButtonGroup
};
